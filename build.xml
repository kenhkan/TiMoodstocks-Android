<project name="timoodstocks-android" default="dist">
	<description>
		Ant build script for Titanium Android module timoodstocks-android
	</description>

	<property name="ti.module.root" location="${basedir}"/>
	<property file="build.properties" />

  <copy todir="libs">
    <fileset dir="so">
      <include name="**/*.so"/>
    </fileset>
  </copy>

	<import file="${titanium.platform}/../module/android/build.xml"/>
</project>
